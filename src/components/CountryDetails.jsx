import { useParamas } from 'react-router-dom';

function CountryDetails(actualCountries) {

  // const params = useParamas();
  // let {alpha3code} = params
 

 
  return(
<div>
    <h2>Detalles de paises</h2>
  </div>

  )
  
}

export default CountryDetails;
